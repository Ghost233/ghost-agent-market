interface:
  display_name: "任务 DAG 规划"
  short_description: "生成并校验 Codex v3 任务 DAG 计划"
  default_prompt: "使用 $parallel-task-planner 生成 v3 任务 DAG；默认使用 gpt-5.6-terra/medium，生成稳定的 logical_id 和 title，并在 revision 中映射可复用旧线程。"
