interface:
  display_name: "任务 DAG 规划"
  short_description: "生成并校验 Codex v3 任务 DAG 计划"
  default_prompt: "使用 $parallel-task-planner 按稳定领域 module 生成 v3 任务 DAG；标注 work、review 或 verify，先做闭包审计，再合并同一静止点的修订。"
