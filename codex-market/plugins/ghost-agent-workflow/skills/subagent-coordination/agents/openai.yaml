interface:
  display_name: "子代理 DAG 协调"
  short_description: "使用 Terra/xhigh 子代理一次性执行已校验的 v3 任务 DAG"
  default_prompt: "使用 $subagent-coordination，通过固定的 gpt-5.6-terra/xhigh 子代理执行已校验且已明确选择子代理模式的当前 DAG。"
policy:
  allow_implicit_invocation: false
