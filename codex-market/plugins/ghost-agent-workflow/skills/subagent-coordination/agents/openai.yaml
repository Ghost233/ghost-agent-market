interface:
  display_name: "Goal DAG 子代理控制器"
  short_description: "在 Codex 原生 Goal 中执行永久 Owner 与 v5 DAG。"
  default_prompt: "/goal 每轮使用 $subagent-coordination，完整执行 `./plan.md`。"

policy:
  allow_implicit_invocation: false
