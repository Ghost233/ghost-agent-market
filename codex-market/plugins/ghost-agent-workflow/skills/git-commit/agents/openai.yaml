interface:
  display_name: "Git 智能提交"
  short_description: "用只读 worker 分析改动并按 submodule 顺序安全创建中文提交"
  default_prompt: "使用 $git-commit 先让只读 worker 分析当前 checkout，再由主线程复核并按最深 submodule 到主工程的顺序，用显式路径分批创建中文提交。"
