interface:
  display_name: "Task 执行子线程"
  short_description: "执行并验证一个 Codex v3 task"
  default_prompt: "使用 $thread-goal-worker 完成绑定 task；scope 变化时返回含 split_hints/overlap_hints 的 scope_request，交主线程自主重规划。"
