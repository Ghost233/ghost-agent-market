interface:
  display_name: "单模块执行子代理"
  short_description: "校验 Codex 实现子代理分派并在限定范围内执行单模块任务"
  default_prompt: "使用 $thread-goal-worker 校验 coordinator 的单 module implementation subagent 分派，确认 profile dispatch evidence 与 active goal 后实现、验证并返回 WORKER_RESULT。"
