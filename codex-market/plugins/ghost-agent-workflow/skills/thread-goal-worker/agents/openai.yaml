interface:
  display_name: "Thread Goal Worker"
  short_description: "子线程执行单一 scope，支持轻量并发模块模式"
  default_prompt: "使用 $thread-goal-worker 执行一个 Codex 子线程任务；收到 parallel-plan module 时先确认 active /goal，再按 scope 实现、验证、diff 自检并最多修复一次，最后返回 WORKER_RESULT。"
