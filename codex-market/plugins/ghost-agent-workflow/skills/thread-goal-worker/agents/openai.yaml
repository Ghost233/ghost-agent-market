interface:
  display_name: "单模块执行子线程"
  short_description: "在受限范围内执行并验证一个 Codex 子线程模块"
  default_prompt: "使用 $thread-goal-worker 验证 v2 绑定包、当前模块子线程的 goal 和 gpt-5.6-terra/xhigh 创建证据，在 writable_paths 内实现、验证并返回 WORKER_RESULT。"
