interface:
  display_name: "任务执行线程"
  short_description: "按角色执行 Codex v3 任务并持久化结果"
  default_prompt: "使用 $thread-goal-worker 完成唯一绑定任务；work 限域实施，review 只读审查，verify 只读验证，并把 WORKER_RESULT_V3 原子写入 result_path。"
