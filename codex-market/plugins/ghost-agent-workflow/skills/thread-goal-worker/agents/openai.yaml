interface:
  display_name: "Thread Goal Worker"
  short_description: "校验 Codex 计划来源与 profile 后执行单模块"
  default_prompt: "使用 $thread-goal-worker 校验 Codex 单 module 的计划来源和运行时 profile，设置 active goal 后执行、验证并返回 WORKER_RESULT。"
