interface:
  display_name: "子线程 DAG 协调"
  short_description: "使用 Codex 子线程一次性执行已校验的 v3 任务 DAG"
  default_prompt: "使用 $thread-coordination 执行已校验且已明确选择子线程模式的当前 DAG。"
policy:
  allow_implicit_invocation: false
