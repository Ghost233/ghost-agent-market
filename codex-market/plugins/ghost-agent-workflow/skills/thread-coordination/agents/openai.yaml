interface:
  display_name: "子线程 DAG 协调"
  short_description: "使用 Codex 子线程一次性执行已校验的 v3 任务 DAG"
  default_prompt: "使用 $thread-coordination 以子线程模式一次性完成已校验的 parent_goal，并只在当前 parent_goal 内复用 module+role 子线程。"
