interface:
  display_name: "Thread 协调"
  short_description: "按 ownership 协调 worker，或执行安全并发计划"
  default_prompt: "使用 $thread-coordination 协调 Codex worker：普通任务按目标域 ownership 拆分；收到 parallel_safe 计划时使用 parallel-plan 模式按 batch 分派 worker、收集 WORKER_RESULT、做总校验，并且每个模块最多补修一次。"
