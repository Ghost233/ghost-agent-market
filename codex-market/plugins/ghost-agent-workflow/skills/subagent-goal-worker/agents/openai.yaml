interface:
  display_name: "子代理任务执行"
  short_description: "在 Sol/medium 子代理中执行范围受控的单个 v3 DAG 任务"
  default_prompt: "使用 $subagent-goal-worker，以固定的 gpt-5.6-sol/medium profile 处理协调器发送的完整单任务绑定包。"
policy:
  allow_implicit_invocation: false
