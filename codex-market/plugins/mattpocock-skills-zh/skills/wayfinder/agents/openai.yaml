# 平台说明：Codex 使用 policy.allow_implicit_invocation: false 对应 Claude Code 的 disable-model-invocation: true。
interface:
  display_name: "探路 (Wayfinder)"
  short_description: "把单次会话容纳不下的大型工作绘制为共享决策工单地图"
  default_prompt: "使用 $wayfinder 把这项大型工作绘制成共享的决策工单地图。"
policy:
  allow_implicit_invocation: false
