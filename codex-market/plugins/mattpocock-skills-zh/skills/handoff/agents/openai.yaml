# 平台说明：Codex 使用 policy.allow_implicit_invocation: false 对应 Claude Code 的 disable-model-invocation: true。Claude Code 的 argument-hint 在 Codex 中没有对应字段。
interface:
  display_name: "会话交接"
  short_description: "将当前对话压缩为可移植的交接文档，供下一位代理继续"
  default_prompt: "使用 $handoff 把当前对话整理成可供下一位代理继续的交接文档。"
policy:
  allow_implicit_invocation: false
