interface:
  display_name: "Git 智能提交"
  short_description: "用 git_commit_worker 分析改动并安全创建中文提交"
  default_prompt: "使用 $git-commit 先按名称启动 git_commit_worker；验证其绑定 gpt-5.3-codex-spark/high 且不传 reasoning summary 或 service tier 参数。worker 成功后由主线程复核并按最深 submodule 到主工程的顺序，用显式路径分批创建中文提交。"
