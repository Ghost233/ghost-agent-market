interface:
  display_name: "Git 智能提交"
  short_description: "检查 staged、unstaged 与 submodule 改动并安全创建中文提交"
  default_prompt: "使用 $git-commit 分析当前 checkout 的 staged、unstaged 和 submodule 改动，按最深 submodule 到主工程的顺序，用显式路径分批创建中文提交。"
