interface:
  display_name: "Task DAG 规划"
  short_description: "生成 Claude Code v3 module/task DAG 计划"
  default_prompt: "使用 $parallel-task-planner 在项目内生成 v3 task DAG JSON，module 默认使用 sonnet/max，通过驱动脚本校验后再决定是否交给 $thread-coordination。"
