interface:
  display_name: "Task DAG 规划"
  short_description: "生成 Claude Code v3 module/task DAG 计划"
  default_prompt: "使用 $parallel-task-planner 生成 v3 task DAG，module 默认使用 sonnet/max；修订时并行拆分大 scope，并把交叉职责抽成共享前置 task。"
