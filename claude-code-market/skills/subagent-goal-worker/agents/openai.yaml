interface:
  display_name: "子代理任务执行"
  short_description: "在 Claude Code 子代理中完成范围受控的单个 v3 DAG 任务"
  default_prompt: "使用 $subagent-goal-worker 处理协调器发来的完整 v3 单任务绑定包。"
policy:
  allow_implicit_invocation: false
