interface:
  display_name: "Thread Goal Worker"
  short_description: "队员执行单一 scope，支持轻量并发模块模式"
  default_prompt: "使用 $thread-goal-worker 执行一个 Claude Code 队员任务；收到 parallel-plan module 时按 scope 实现、验证、diff 自检并最多修复一次，最后返回 WORKER_RESULT。"
