interface:
  display_name: "Thread Goal Worker"
  short_description: "Claude Code team 队员按 scope 执行并自审查"
  default_prompt: "使用 $thread-goal-worker 作为 Claude Code agent team 队员接收 coordinator 分派：先确认 parent_goal、owner_domain、goal_id、scope、constraints 和 verification，再在授权范围内执行；完成后进行验证、自审查、审查后修复和自验收，再返回 TEAMMATE_RESULT。"
