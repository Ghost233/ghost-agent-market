interface:
  display_name: "Task Worker 执行"
  short_description: "执行并验证一个 Claude Code v3 task"
  default_prompt: "使用 $thread-goal-worker 验证 v3 task 分派、module profile 和 assignment evidence，仅在 writable_paths 内实现、验证和 diff 自检，然后返回 WORKER_RESULT_V3。"
