interface:
  display_name: "Task Worker 执行"
  short_description: "执行并验证一个 Claude Code v3 task"
  default_prompt: "使用 $thread-goal-worker 完成分派 task；scope 变化时返回含 split_hints/overlap_hints 的 scope_request，交主 session 自主重规划。"
